<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <base href="/stager/"/>
    <title>ChattyStager</title>
    <ResourcePreloader/>
    <link rel="stylesheet" href="css/tailwind.css" />
    <link rel="stylesheet" href="@Assets["ChattyStager.styles.css"]"/>
    <ImportMap/>
    <link rel="icon" type="image/png" href="favicon.png"/>
    <HeadOutlet/>
</head>
 
<body class="w-full min-h-screen">
<Routes/>
<ReconnectModal/>
<script src="@Assets["_framework/blazor.web.js"]"></script>
<script>
(function () {
    window.showNavigationProgress = function () {
        const bar = document.querySelector('.navigation-progress');
        if (bar) bar.classList.remove('navigation-progress--done');
    };

    window.hideNavigationProgress = function () {
        const bar = document.querySelector('.navigation-progress');
        if (bar) bar.classList.add('navigation-progress--done');
    };

    // --- Scroll helper ---
    window.scrollElementToBottom = function (el) {
        if (el) el.scrollTop = el.scrollHeight;
    };

    // --- NavLink click → show progress (native, bypasses Blazor lifecycle) ---
    document.addEventListener('click', function (e) {
        if (e.target.closest('.nav-link')) {
            window.showNavigationProgress();
        }
    });
})();
</script>
</body>

</html>